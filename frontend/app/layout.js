import './globals.css';
export const metadata = { title: 'AEGIS', description: 'Immune system for digital organisms' };
export default function RootLayout({ children }) {
  return <html lang="ru"><body>{children}</body></html>;
}
